---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: ucore-hci
description: A basic uCore image with some tweaks and pre-installed packages.

base-image: ghcr.io/ublue-os/ucore-hci
image-version: stable

alt-tags:
  - stable-zfs

modules:
  - from-file: server/ucore/base.yaml
